{% from './dashboard-section-task.njk' import dashboardSectionTask %}

{% macro dashboardSection(section) %}
  <h2 class="app-task-list__section">
    {{ section.title }}
  </h2>
  <ul class="app-task-list__items">
    {% for task in section.tasks %}
      {{ dashboardSectionTask(task) }}
    {% endfor %}
  </ul>
{% endmacro %}
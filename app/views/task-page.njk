{% extends 'includes/layout.njk' %}
{% from './task-question.njk' import taskQuestion %}

{% block body %}
<div class="nhsuk-grid-row">
  <div class="nhsuk-grid-column-full">

    <h1>{{ title }}</h1>

    {% for question in questions%}
      {{ taskQuestion(question) }}
    {% endfor %}

  </div>
</div>
{% endblock %}

import { runTestSuite } from '../../../../../test-utils/runTestSuite';

const sectionId = 'integrations';

const integrationsMarketingData = {
  link: 'The supplier asserted integrations link',
};

runTestSuite({
  data: integrationsMarketingData,
  sectionId,
});
